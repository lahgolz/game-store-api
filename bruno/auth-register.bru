meta {
  name: auth-register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "dev@acme.com"
  }
}

settings {
  encodeUrl: true
}
